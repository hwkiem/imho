mutation CreatePendingUser($input: PendingUserInput!) {
    createPendingUser(input: $input) {
        result {
            id
            email
            createdAt
        }
        errors {
            field
            error
        }
    }
}
