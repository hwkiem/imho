mutation RegisterUser($input: RegisterInput!) {
    registerUser(input: $input) {
        result {
            id
            createdAt
            email
        }
        errors {
            field
            error
        }
    }
}
