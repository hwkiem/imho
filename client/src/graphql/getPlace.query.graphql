query GetPlace($placeId: String!) {
    getPlace(placeId: $placeId) {
        result {
            id
            createdAt
            google_place_id
            formatted_address
            topNFlags(n: 5) {
                pros {
                    topic
                    cnt
                }
                cons {
                    topic
                    cnt
                }
                dbks {
                    topic
                    cnt
                }
            }
            residences {
                id
                createdAt
                unit
                reviews {
                    id
                    createdAt
                    feedback
                }
            }
        }
        errors {
            field
            error
        }
    }
}
