mutation WriteReview($options: WriteReviewInput!) {
    writeReview(options: $options) {
        errors {
            ...RegularError
        }
        review {
            ...RegularReview
        }
    }
}
