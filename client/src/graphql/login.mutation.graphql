mutation Login($input: LoginInput!) {
    login(input: $input) {
        result {
            id
            createdAt
            email
        }
        errors {
            field
            error
        }
    }
}
